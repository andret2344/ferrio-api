<div class="pagination-button{{ disabled ? ' disabled' : '' }}">
	{% if disabled %}
		<div class='holder'>
			<div>{{ text | raw }}</div>
		</div>
	{% else %}
		<a class='holder' href="{{ url }}" rel="{{ rel }}">
			<div>{{ text | raw }}</div>
		</a>
	{% endif %}
</div>
